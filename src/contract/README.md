# src/contract

**Owner:** `contract-architect` (WP-02). Frozen after Gate G1: changes need a `contractVersion` bump, a JSON Schema re-export and a `docs/DECISIONS.md` entry.

Rule Set contract: TypeScript types, Zod schemas, JSON Schema export, version rules (docs/PLAN.md §5).
