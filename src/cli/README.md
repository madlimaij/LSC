# src/cli

**Owner:** `contract-architect` (WP-01) for the bootstrap files (`index.ts`, `command-module.ts`, `package-info.ts`). Each file in `commands/` is owned by the work package that adds it.

Command wiring. Do not register commands by editing `index.ts`; add a file to `commands/` instead (see `commands/README.md`).
